欢迎收听每日算法语音讲解。今天是第 76 题，最小覆盖子串。题型是可变滑窗，难度困难。本次讲解预计时长 约 5 到 6 分钟，共 7 个部分。内容结构如下：第一，题面描述和输入输出示例；第二，思考过程，共 4 步；第三，变量语义，共 3 个核心变量；第四，落码步骤，共 4 步；第五，时间与空间复杂度；第六，常见坑点，共 3 条；第七，边界情况，共 3 条。好，我们开始。

题目描述。给你一个字符串 s、一个字符串 t。返回 s 中涵盖 t 所有字符的最小子串。如果 s 中不存在涵盖 t 所有字符的子串，则返回空字符串 ""。

示例。示例 1

    输入：s = "ADOBECODEBANC", t = "ABC"

先模拟答题者的思考过程，从暴力解法出发，找到重复劳动，再推导优化方向。

1. 暴力：枚举所有子串检查是否覆盖 t。要滑动窗口就必须回答「可行」（cover），得到「比长度」。

2. 右指针 r 扩张到覆盖为止，一旦覆盖就尝试收左指针 l 缩到最短。

3. 需要 O(1) 判断是否覆盖，所以引入计数表 need/window 和 valid 计数。

4. 当 valid == distinct_chars_in_t 时，当前窗口 [l,r) 是一个可行解，尝试缩小 l 找更短。

接下来是变量语义，先理解每个变量的定义、维护和更新，再开始编码。

变量 need[c]，类型 map<char,int>。定义：目标串 t 对字符 c 的需求次数
维护：不变量，初始化为 t 的字符频率
更新：不更新

变量 window[c]，类型 map<char,int>。定义：当前窗口内字符 c 的计数
维护：随窗口滑动实时反映窗口内容
更新：窗口右扩时 window[c]++，左缩时 window[c]--

变量 valid，类型 int。定义：已满足需求的字符种类数
维护：每轮后 valid = 满足 window[c] >= need[c] 的字符 c 的数量
更新：当 window[c] 刚好等于 need[c]（从 < 变成 =）时 valid++

落码步骤如下。

1. 统计 need：遍历 t，对每个字符 need[c]++

2. 右指针 r 扩张：window[s[r]]++，若 window[s[r]] == need[s[r]] 则 valid++

3. 当 valid == len(need)（全部满足），循环收缩左指针：更新答案，window[s[l]]--，若 window[s[l]] < need[s[l]] 则 valid--，l++

4. 返回最短子串或空串

复杂度分析。时间复杂度 O(m+n)，空间复杂度 O(|Σ|)。

常见坑点，请注意。

&#x2757; valid 计数逻辑：必须 window[c] 刚好等于 need[c] 时才 valid++，超过不算（否则重复字符会虚增 valid）。

&#x2757; 收缩时更新顺序：先判断 window[d]==need[d] 再 window[d]--，和扩张时顺序相同。

&#x2757; 返回空串的判断：min_len 仍是 INF 说明从未形成合法窗口。

必测的边界情况。

Case 1：s 和 t 相同

Case 2：t 比 s 长

Case 3：t 有重复字符

讲解完毕。建议回到网页查看完整代码实现，动手写一遍加深理解。祝学习顺利！