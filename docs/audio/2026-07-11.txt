欢迎收听每日算法语音讲解。今天是第 5 题，最长回文子串。题型是中心扩展，难度中等。本次讲解预计时长 约 3 到 4 分钟，共 7 个部分。内容结构如下：第一，题面描述和输入输出示例；第二，思考过程，共 4 步；第三，变量语义，共 2 个核心变量；第四，落码步骤，共 4 步；第五，时间与空间复杂度；第六，常见坑点，共 3 条；第七，边界情况，共 2 条。好，我们开始。

题目描述。给你一个字符串 s，找到 s 中最长的回文子串。回文是指正着读和反着读都一样的字符串。

示例。示例 1

    输入：s = "babad"。示例 2

    输入：s = "cbbd"

先模拟答题者的思考过程，从暴力解法出发，找到重复劳动，再推导优化方向。

1. 暴力：枚举所有子串再逐个判断是否回文，O(n³)。

2. 回文有对称性：从中心往两边扩展，天然是回文，不必重复判断。

3. 中心有两类：奇数长度以单个字符为中心，偶数长度以两个字符之间为中心。

4. 枚举每个中心（共 2n-1 个）向外扩展，取最长的一段，O(n²) 时间、O(1) 空间。

接下来是变量语义，先理解每个变量的定义、维护和更新，再开始编码。

变量 l, r，类型 int。定义：从某中心向两侧扩展时的左右指针
维护：扩展过程中 s[l..r] 始终是回文
更新：只要 s[l]==s[r] 就 l--、r++

变量 start,end，类型 int。定义：目前发现的最长回文子串区间
维护：始终记录最长的一段
更新：某次扩展得到更长回文时更新

落码步骤如下。

1. 写一个 expand(l, r)：当 s[l]==s[r] 时 l--、r++，返回最长回文区间

2. 遍历每个 i：以 (i, i) 为中心求奇数回文

3. 以 (i, i+1) 为中心求偶数回文

4. 用更长者更新 start, end，最后返回 s[start:end+1]

复杂度分析。时间复杂度 O(n²)，空间复杂度 O(1)。

常见坑点，请注意。

&#x2757; 别漏掉偶数长度中心 (i, i+1)，否则 "bb" 这类会漏解。

&#x2757; 扩展结束时循环多走了一步，真正的回文区间是 [l+1, r-1]。

&#x2757; 空串或单字符要能正确返回（maxLen 初始设 1，空串单独处理）。

必测的边界情况。

Case 1：整体回文

Case 2：无长回文

讲解完毕。建议回到网页查看完整代码实现，动手写一遍加深理解。祝学习顺利！