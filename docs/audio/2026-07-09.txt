欢迎收听每日算法语音讲解。今天是第 3 题，无重复字符的最长子串。题型是可变滑窗，难度中等。本次讲解预计时长 约 4 到 5 分钟，共 7 个部分。内容结构如下：第一，题面描述和输入输出示例；第二，思考过程，共 4 步；第三，变量语义，共 3 个核心变量；第四，落码步骤，共 4 步；第五，时间与空间复杂度；第六，常见坑点，共 3 条；第七，边界情况，共 2 条。好，我们开始。

题目描述。给定一个字符串 s，请你找出其中不含有重复字符的最长子串的长度。

示例。示例 1

    输入：s = "abcabcbb"。示例 2

    输入：s = "pwwkew"

先模拟答题者的思考过程，从暴力解法出发，找到重复劳动，再推导优化方向。

1. 暴力：枚举所有子串再判断是否有重复，O(n³) 或 O(n²)。

2. 重复在哪？right 右移时，其实只有「新加入的字符」可能造成重复。

3. 用滑动窗口：right 不断右扩，一旦 s[right] 在窗口内出现过，就把 left 跳过去。

4. 关键：记录每个字符最近的下标，跳 left 时只能往右（用 max/判断 last[c] >= left），不能倒退。

接下来是变量语义，先理解每个变量的定义、维护和更新，再开始编码。

变量 left，类型 int。定义：当前无重复窗口的左边界
维护：窗口 [left, right] 内永远无重复字符
更新：遇到重复字符时，跳到该字符上次出现位置的右侧

变量 last[c]，类型 map<char,int>。定义：字符 c 最近一次出现的下标
维护：随扫描实时更新
更新：每轮 last[s[right]] = right

变量 ans，类型 int。定义：无重复子串的最大长度
维护：所有合法窗口长度的最大值
更新：ans = max(ans, right - left + 1)

落码步骤如下。

1. last = {}，left = 0，ans = 0

2. 遍历 right：若 s[right] 在 last 中且 last[c] >= left，则 left = last[c] + 1

3. 更新 last[s[right]] = right

4. ans = max(ans, right - left + 1)

复杂度分析。时间复杂度 O(n)，空间复杂度 O(|Σ|)。

常见坑点，请注意。

&#x2757; 跳 left 前必须判断 last[c] >= left：字符虽出现过但若在窗口左侧之外，不能把 left 往回拉。

&#x2757; 先跳 left 再更新 last[c]，顺序不能反。

&#x2757; 窗口长度是 right - left + 1，不要漏掉 +1。

必测的边界情况。

Case 1：空串

Case 2：全相同

讲解完毕。建议回到网页查看完整代码实现，动手写一遍加深理解。祝学习顺利！