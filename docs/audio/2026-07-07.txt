欢迎收听每日算法语音讲解。今天是第 1 题，两数之和。题型是哈希表，难度简单。本次讲解预计时长 约 4 到 5 分钟，共 7 个部分。内容结构如下：第一，题面描述和输入输出示例；第二，思考过程，共 4 步；第三，变量语义，共 2 个核心变量；第四，落码步骤，共 4 步；第五，时间与空间复杂度；第六，常见坑点，共 3 条；第七，边界情况，共 2 条。好，我们开始。

题目描述。给定一个整数数组 nums 和一个整数目标值 target，请你在该数组中找出和为目标值 target 的那两个整数，并返回它们的数组下标。你可以假设每种输入只会对应一个答案，且同一个元素不能使用两遍。

示例。示例 1

    输入：nums = [2,7,11,15], target = 9。示例 2

    输入：nums = [3,2,4], target = 6

先模拟答题者的思考过程，从暴力解法出发，找到重复劳动，再推导优化方向。

1. 暴力：双重循环枚举所有 (i, j) 看和是否为 target，O(n²)。

2. 重复在哪？对每个 x 都在「重新遍历」找 target - x，其实只要知道它之前是否出现过。

3. 把「找另一半」变成查表：用哈希表存已扫过的值到下标，查找变 O(1)。

4. 边扫边存：先查 target - x 是否在表里，再把 x 存入，保证不会用到自己。

接下来是变量语义，先理解每个变量的定义、维护和更新，再开始编码。

变量 seen，类型 map<int,int>。定义：已扫描过的「值 ，得到 下标」映射
维护：每轮结束后，seen 里存着 nums[0..i] 每个值最后出现的下标
更新：处理完 nums[i] 后 seen[nums[i]] = i

变量 target - x，类型 int。定义：当前元素 x 需要的「另一半」
维护：随 x 变化
更新：每轮用它去 seen 里查是否出现过

落码步骤如下。

1. 初始化空哈希表 seen

2. 遍历数组，对每个 x = nums[i]：先查 target - x 是否在 seen 中

3. 若在，返回 [seen[target-x], i]

4. 否则把 seen[x] = i 记入历史

复杂度分析。时间复杂度 O(n)，空间复杂度 O(n)。

常见坑点，请注意。

&#x2757; 必须「先查后存」：如果先把 x 存进表再查，可能会把自己当成另一半（当 target = 2*x 时）。

&#x2757; 返回的是下标不是值；题目保证恰有一个答案，无需继续遍历。

&#x2757; 有重复值时哈希表会覆盖旧下标，但因为答案唯一，不影响正确性。

必测的边界情况。

Case 1：包含负数

Case 2：两个相同值

讲解完毕。建议回到网页查看完整代码实现，动手写一遍加深理解。祝学习顺利！