欢迎收听每日算法语音讲解。今天是第 206 题，反转链表。题型是链表指针，难度简单。本次讲解预计时长 约 4 到 5 分钟，共 7 个部分。内容结构如下：第一，题面描述和输入输出示例；第二，思考过程，共 4 步；第三，变量语义，共 3 个核心变量；第四，落码步骤，共 5 步；第五，时间与空间复杂度；第六，常见坑点，共 3 条；第七，边界情况，共 2 条。好，我们开始。

题目描述。给你单链表的头节点 head ，请你反转链表，并返回反转后的链表。

示例。示例 1

    输入：head = [1,2,3,4,5]

先模拟答题者的思考过程，从暴力解法出发，找到重复劳动，再推导优化方向。

1. 直观想法：我需要把每个节点的 next 指针反过来指向前一个节点。

2. 关键问题：改了 cur.next 之后，我就找不到原来的下一个节点了——所以在改之前必须用 nxt 保存。

3. 初始状态：第 0 个位置是 null（pre = None），第 1 个是 head（cur = head）。

4. 循环结束后 pre 就指向了新的头节点（原链表的尾）。

接下来是变量语义，先理解每个变量的定义、维护和更新，再开始编码。

变量 pre，类型 ListNode*。定义：已反转部分的头节点
维护：始终指向已反转链表的首节点
更新：每轮 pre = cur（cur 被接入反转链表头部）

变量 cur，类型 ListNode*。定义：当前正在处理的节点
维护：指向原链表中下一个待反转的节点
更新：每轮 cur = nxt（移到下一个）

变量 nxt，类型 ListNode*。定义：cur 的原后继（改指针前先保存，防断链）
维护：每轮保存 cur.next
更新：nxt = cur.next（在 cur.next 被改写之前）

落码步骤如下。

1. pre = None, cur = head

2. 循环：nxt = cur.next（保存后继，防断链）

3. cur.next = pre（反转指针）

4. pre = cur; cur = nxt（两指针前移）

5. 返回 pre

复杂度分析。时间复杂度 O(n)，空间复杂度 O(1)。

常见坑点，请注意。

&#x2757; 顺序！必须先 nxt = cur.next 保存，再修改 cur.next。反过来就会「断链」——丢失后续所有节点。

&#x2757; 返回值是 pre 不是 cur：循环结束时 cur 是 None，pre 才是新头。

&#x2757; 空链表 / 单节点：while 直接跳过，返回 pre（null 或 head），行为正确。

必测的边界情况。

Case 1：空链表

Case 2：单节点

讲解完毕。建议回到网页查看完整代码实现，动手写一遍加深理解。祝学习顺利！